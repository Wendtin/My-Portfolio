name: The First Pulse
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  test-runner:
    runs-on: ubuntu-latest
    steps:
      - name: Print Status
        run: echo "The Conveyor Belt is Operational. Hello DevSecOps!"
